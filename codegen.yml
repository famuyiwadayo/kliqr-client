overwrite: true
schema: "http://localhost:3221/graphql"
documents: "src/**/*.{ts,tsx}"
generates:
  src/generated/graphql.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
